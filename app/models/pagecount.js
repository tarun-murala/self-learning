/*var mongoose = require('mongoose');

var Schema = mongoose.Schema;

var pageCountSchema = new Schema({
    count: {type : String, default: 0}
});

module.exports = mongoose.model('pagecount', pageCountSchema);*/